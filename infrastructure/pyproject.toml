[project]
name = "infrastructure"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pulumi>=3,<4",
    "pulumi-docker>=4,<5",
    "pulumi-postgresql>=3,<4",
    "pulumi-random>=4,<5",
]
