# Cluster Setup

## Installing k3s

TODO: Document k3s installation steps and any custom configuration flags used.

## Verifying the Cluster

After installation, verify the cluster is running:

```bash
kubectl get nodes
kubectl get pods -A
```
