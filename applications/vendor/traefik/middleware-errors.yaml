apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: default-errors
  namespace: traefik
spec:
  errors:
    status: ["401", "403", "404", "500-599"]
    service:
      name: error-pages
      namespace: error-pages
      port: 8080
    query: "/{status}.html"
