---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: glance
  namespace: glance
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`dashboard2.neustrom.net`)
      kind: Rule
      services:
        - name: glance
          port: 8080
