---
database:
  type: postgresql

dbCredentials:
  existingSecret: "homarr-db-secret"
  dbUrlKey: "DB_URL"

dbEncryption:
  existingSecret: "homarr-db-encryption"
  key: "DB_ENCRYPTION_KEY"

env:
  TZ: "Europe/Stockholm"

ingress:
  enabled: false
