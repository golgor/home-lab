---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: pihole-redirect-admin
  namespace: pihole
spec:
  redirectRegex:
    regex: "^https://pihole\\.neustrom\\.net/?$"
    replacement: "https://pihole.neustrom.net/admin"
    permanent: true
